/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['imgur.com'],
  },
  turbopack: {
    // You can enable/disable options here if needed
  },
};

export default nextConfig;
